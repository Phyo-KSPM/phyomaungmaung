# Copy to .env.local for local dev. Never commit real secrets.
# Leave MONGODB_URI empty → local login uses kophyo / Letmein+123 without DB (dev only).
MONGODB_URI=
users_MONGODB_URI=
JWT_SECRET=
ACCESS_CODE_SECRET=
# Optional: force no-DB login even if MONGODB_URI is set (local only)
# DEV_LOGIN_NO_DB=1
